# vscode

Unified VS Code Stow package for both Linux and macOS.

- Canonical settings live at `.config/Code/User/settings.json`.
- macOS uses `Library/Application Support/Code/User/settings.json`, which is a symlink to the canonical file in this package.
- `extensions.txt` is the shared extension list.
